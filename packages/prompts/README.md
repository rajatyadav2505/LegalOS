# Prompts Package

Versioned prompt assets and prompt fixtures live here.

The current repository uses deterministic template assets for:

- Phase 3 drafting document schemas and section layouts
- Phase 4 bounded strategy branch scaffolds

Provider-backed prompt execution is still intentionally isolated behind future adapters rather than embedded in application code.
